preprocessing:
    epoching:
        reject: null
        in_names: [ps, kt]
        in_numbers: [60, 70]
        out_names: [[PSKT], [ps, kt]]
        out_numbers: [[60, 60],  # Combine all trials
                      [60, 70]]  # Separate trials
